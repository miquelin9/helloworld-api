package com.modeling;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeExampleResourceIT extends HelloControllerTest {

    // Execute the same tests but in native mode.
}